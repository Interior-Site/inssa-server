version: "3"

services:
  db:
    image: mariadb:10
    ports:
      - 3306:3306
    volumes:
      - ./db/conf.d:/etc/mysql/conf.d
      - ./db/initdb.d:/docker-entrypoint-initdb.d
      - ./db/mysql/:/var/lib/mysql
    env_file: .env
    environment:
      TZ: Asia/Seoul
    restart: always

networks:
  backend: